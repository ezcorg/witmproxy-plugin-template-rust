[package]
name = "{{project-name}}"
version = "0.1.0"
edition = "2024"
authors = ["{{author}}"]
description = "{{description}}"

[lib]
crate-type = ["cdylib"]

[dependencies]
wit-bindgen = "0.47.0"

[package.metadata.component]
package = "witmproxy:{{project-name}}-plugin"

[package.metadata.component.dependencies]

[package.metadata.component.target.dependencies]